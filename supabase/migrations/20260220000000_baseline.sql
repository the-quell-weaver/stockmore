-- Baseline migration for Stockmore.
--
-- All schema, RLS, policy, index, and constraint changes must be added
-- as versioned SQL files under supabase/migrations.

begin;

-- Intentionally left blank. Future DB changes must be introduced as
-- forward-only migrations in new files.

commit;
